import { DataTableItemsPipe } from './data-table-items.pipe';

describe('DataTableItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new DataTableItemsPipe();
    expect(pipe).toBeTruthy();
  });
});

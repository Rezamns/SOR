import { EnableIncrementDecrementStatusPipe } from './enable-increment-decrement-status.pipe';

describe('EnableIncrementDecrementStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EnableIncrementDecrementStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
